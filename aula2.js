// const = variável constante, o valor armazenado não pode ser alterado
// let = variável local
// var = variável global
// Calculadora em JS
const v1 = 1500
const v2 = "2"

const soma = v1 + Number(v2)
const sub = v1 - v2
const mul = v1 * v2
const div = v1 / v2

console.log("A soma é: " + soma)
console.log("A subtração é: " + sub)
console.log("A multiplicação é: " + mul)
console.log("A divisão é: " + div)